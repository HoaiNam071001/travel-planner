// Lane "kho hoạt động chưa gắn chặng" trong Plan Builder — id giả, không phải
// unit_id thật, để phân biệt với các lane là chặng.
export const LIBRARY_LANE = "library";

export type LaneId = string;
